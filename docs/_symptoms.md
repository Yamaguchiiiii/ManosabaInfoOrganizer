確認した症状：

# 05/15_19:50
1. Syncを行った際に合流してきた人の速度が合わないで置いていかれたり追い越されたりする．
2. VSCode上で開いたり，tauri上で開くと問題はないが，Chrome 上でOOMエラーが確認されます．　処理待ち->動くの繰り返しをしているように見えるアニメーションの挙動をしていました．
3. Animateページでキャラクターが階層を移動して，動先のマップに表示された際に，そのマップ上での移動開始位置と異なる場所にキャラクターアイコンが表示され，その後直線的に正しい経路の方向に移動する（階層移動時の座標のやり取りがうまくいっていない？）

# 05/16_15:00
1. VCode上，tauri上，Edge上ではアニメーションの再生が滑らかであるが，Chrome上ではアニメーションがカクついて時間がたつとOOMエラーが出る
2. 追加したキャンバスのレイヤー機能を使用するためのUIをToolバーに表示する．
3. 追加したキャンバスのレイヤー機能は画像などのキャンバス上の全オブジェクトに適用する．(現在画像オブジェクト上で右クリックをしてもフリーライティングオブジェクトや図形オブジェクトのようにレイヤー設定のフローティングウィンドウが表示されない)
4. Animate上のキャラクターアイコンの縁色は変更されたが，Create上のトップバーのキャラアイコン選択時，およびNote上のキャラクターノート上のトップバーのキャラアイコン選択時の縁色が変更されていない．
5. Noteページで，複数オブジェクトを選択した際に，Toolsにグループ化ボタンを出現させる．
5. Noteページで，複数オブジェクトを選択した際に，選択されたオブジェクト上で右クリックしたらフローティングウィンドウが表示され，グループ化を行う設定ボタンが表示されるようにしたい．
6. Noteページで，グループ化されたオブジェクトおよび，複数選択したオブジェクトのundoを行った際に，オブジェクト一個一個ごとにundoされ，選択した（あるいはグループ化された）オブジェクト数回数undoしなければならない
7. Noteページで，グループ化されたオブジェクトを選択した際に，Toolsにグループ化解除アイコンを表示させる．
8. Noteページで，グループ化されたオブジェクトのグループ化を解除した際には，ユーザーにグループ化が解除したことがわかりやすいよう，各オブジェクトのバウンディングボックスを独立に表示する．
9. Notes ページのキャンバスで，「図形や画像を追加する際，単にクリックされた場合は現在の実装のようにそのまま配置してよいが，ドラッグされた場合には，大きさをドラッグされているマウスポインタの位置まで拡大を行い，ドラッグが終了した際に図形オブジェクトとして確定させる」機能について，ドラッグした際の青枠バウンディングボックスはよいが，ドラッグしても単にクリックされた場合と同様の図形および画像の追加方法になっており，画像や図形がドラッグされているマウスポインタ領域で拡大されて追加されない．また，ドラッグ時のプレビュー描画に現在の地点でドラッグを終了してマウスクリックを離すと，どのような画像や図形が描画されるかをリアルタイムに表示するようにするべき．
10. Note ページのキャンバスで図形を選択し，アスペクト比が変わる変形を行った際に元のアスペクト比に戻ってしまう．
11. Note ページのキャンバス上のテキストオブジェクトの拡大縮小機能を削除すべき．

# 05/16_22:00
1. Noteページのキャンバス上で，Textオブジェクトをクリックしてもバウンディングボックスが表示されない
2. Noteページのキャンバス上で，Textオブジェクトと他のオブジェクトをクリックして複数選択状態にして，グループ化を行ってもグルーピングされない
3. Noteページのキャラクターページで，デフォルトで配置する画像が現在public/iconになっているが，これをpublic/characterにする．配置場所は左下にキャラクターの上半身が見える(画像のheightから画像のheight/2)くらいに配置する．
4. 05/16_15:00の問題6が解消されていない．グループ化されたオブジェクトを移動してctrl+zを押しても何も起こらず，もう一度ctrl+zを行うとundoがグループ化されたオブジェクトに一括で行われるが，グループ化も解除されてしまう．
5. Noteページのキャンバス上で，フリーライティングの補正の強さの項目をトグルによる固定値ではなく，widthと同様にスライドバーで調節できるようにする．
6. 05/16_15:00の問題9について，四角形の描画は問題ないが，〇と△についてはドラッグ後，大きさの指定には問題がないが表示位置がバウンディングボックスに収まるようになっておらず最初にクリックされた点に現れる．

# 05/16_22:50
1. Createページでスタートとゴールにもsyncを行う時計アイコンボタンとsync機能が欲しい
2. Noteページのキャンバスで，テキストオブジェクトを他オブジェクトとグループ化はできるが，そのグループをドラッグ操作などしても他オブジェクトだけが移動してテキストオブジェクトは移動しない
3. AnimateページのTimeline notes のキャンバスサイズは，Noteページの対応プリセットページのキャンバスを参照しているのにキャンバスサイズが一致していない．
4. webページが拡大や縮小を行われてもNoteページのキャンバスサイズが変わらないようにしたい．
5. Noteページで画像を読み込む際は，透明化された背景が原因で画像のバウンディングボックスが画像の前景と一致せず大きくとられることを防ぐため，前景が存在する部分でバウンディングボックスを取ってクロップしてから追加するようにする．
6. Noteページのキャンバス上に表示される画像はデフォルトで元アスペクト比を維持するようにする．配置された画像オブジェクトが選択された際にはTools上にアスペクト比を維持するというチェックボックスを用意して，ユーザーがアスペクト比を維持するかどうかを選択できるようにする

# 05/20_19:00
1. 現在，Createページでsync機能を単純な使い方（AをBとすれ違わせる・合流させるだけなど）をしても正常に動作しますが，複雑な使い方（例：AはBとある地点まで同行し，その後そこで少しとどまった後にBと別れて違う地点に向かったところCと出会い，AはCを連れてある地点まで行ったところでDと出会い，CをDに任せてCとDはある地点に同行し，Aは別の場所に一人で行く）をすると，同行やすれ違い等の時点がずれる．
2. 現在のCreateページのsync機能のUIでは複雑な使い方(1．の複雑な使い方の例のようなsyncを多用したり，同行区間を詳細に設定したりする場合)に対応できないため，sync機能をモダンかつ利便性の高いUIに変更してほしい．