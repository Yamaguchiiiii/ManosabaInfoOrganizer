# 引き継ぎ資料 — 作業進捗

最終更新: 2026-06-22

---

## 本セッションの作業（2026-06-22）

`_symptoms.md #06/21 00:17`（最優先2件 + 症状1〜7）に着手。
設計・対応記録は `docs/resolve_error/12.md`。

### 実装した項目（型/ビルド検証済み・実機未検証）

| No | 内容 | 主要ファイル |
|---|---|---|
| 1 | 行動なしキャラの「待受sync」許可（Startのみでも到達時刻=Delayとして続行）＋追従時の合流地点重複除外 | `CreateView.tsx` |
| 2 | キャラ経路削除時に他キャラの `syncConstraints` から当該IDを除去（残存sync表示の解消） | `store.ts` |
| 3 | Canvasオブジェクトの切り取り（✂️ボタン / Ctrl+X） | `NoteView.tsx` |
| 4 | 4ペイン跨ぎドラッグ移動（ドロップ位置ヒットテスト→`canvasIndex`付け替え。統一`onDragEnd`） | `NoteView.tsx` |
| 5 | sync同行時の「少し後をついていく（遅延・秒）」入力。合流地点の `stayTime` 加算で実現 | `CreateView.tsx` |
| 6 | 合流モーダルのUI整理（STEP表示/読みやすい名前/全選択/日本語化）。※全面再設計は方針待ち | `modals/MergeModal.tsx` |
| 7 | 4ペインで呼び出し元ペインをクリックしても戻れる（`!isCurrent` 条件を撤去） | `NoteView.tsx` |

### 追加対応（2回目）

- **最優先A（右下Canvasが点線背景だけ）**: ✅ **原因特定・修正済み**。
  右下セル(`grid-cell control-cell-wrapper`)が `.grid-cell` の点線背景画像を継承したまま
  （`background-color` だけ上書きし `background-image` を消していなかった）で、
  #7 で NotesPanel を撤去後、操作盤(`AnimationTimeline`)が薄いバー1本ぶんしか占めず
  セルの大半が点線の空白になっていた。`.control-cell-wrapper` に `background-image:none`、
  `AnimationTimeline` を `height:100%`+縦中央で充填して暗色の操作パネルに。
  → `AnimateView.scss`, `AnimationTimeline.tsx`。

- **最優先B（アニメのカクツキ）**: ✅ 修正実施。追加報告（「1barに2.5秒」「前後に小刻み振動」「全速度」）から
  主因を **seek誤検出のフィードバックループ** と確定（4フレームに1回しか currentTime を書かないのに
  `timeRef` と storeTime の差で外部シーク判定→低fps時に自分の書込遅延を誤検出して timeRef を巻き戻し＋進行スキップ）。
  - 修正4点: ①seek判定を「自分の最後の書込値との差」に変更(`lastWrittenTimeRef`)＝巻き戻し除去、
    ②ノードは変化時のみ更新＝動かないフロアのレイヤー全再描画を停止、③`MovingCharIcon` の影除去＋画像1枚化、
    ④`currentVisualPositions` インプレース化。
  - 22:46バッチは再生フレーム経路に非接触（誤指摘）。これは05/16からの既存性能問題＋seekバグだった。
  - 要live検証: Chromeで前後振動が消え1barが約1秒で進むか。なお重ければ DevTools Performance 録画。
  - 正規化（minStart）はカクツキ非該当だったため**元に戻し済み**（再実装）。
  - **live結果**: Chrome/Tauri=非常に滑らか（解消）。Edgeブラウザのみ微カクツキ残 → 追加修正:
    ①アイコンを Konva `cache()` でビットマップ化（再描画コスト減）、②LERPを時間ベース化
    （フレーム落ち耐性）。WebView2(=Edgeエンジン)は滑らかなので残りはEdgeブラウザの余力不足が主。
    要再検証（Edge実機）。

### 未解決・要対応（次セッションへ）

- **#6 全面再設計**: 合流モーダルの非破壊改善は実施済み。機能分類した1パネルへの統合は
  粒度の方針確認が必要なため保留。

### 検証

- `node_modules/.bin/tsc --noEmit` ✅ 0件
- `npm run build`（`tsc && vite build`）✅ 成功

---

## コミット状況

- 本セッションの変更は**未コミット**（ユーザー確認待ち）。
- `docs/_symptoms.md`（`#06/21 00:17` 追記分）は前セッションから未コミットのまま。

---

## 次のセッションで最初にやること

1. 実機で最優先A/B（Animate回帰）を再現・原因特定 → 修正。
2. 実装済み #1/#5 の sync 挙動を実機確認（待受sync・遅延追従・重複除外）。
3. #4 / #6 の実装可否と方針をユーザーに確認。
</content>
