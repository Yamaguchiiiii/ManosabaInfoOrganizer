# プロジェクト概要
このプロジェクトは「魔法少女の魔女裁判」をより楽しむための推理整理支援および，よりキャラを好きになれるキャラノート，推理を整理するノートなどのノート機能を提供するアプリです．
最大の特徴は　**「Tauriを用いたPC用デスクトップアプリ」と，「スマートフォンおよびPCブラウザの両方で動くWebサイト」のすべて方で，同じコードベースを利用して動作させるクロスプラットフォーム設計**　であることです．

# 技術スタック
- フロントエンド: React, TypeScript
- スタイリング: SCSS(CSS Modules または BEM記法)
- デスクトップ基盤: Tauri(Rust)
- Web基盤: 標準Web API(Browser)
- パッケージマネージャー: npm

# クロスプラットフォーム・アーキテクチャ(絶対ルール)
Tauri(デスクトップ)環境とブラウザ(Web)環境の両方で動作させるため，以下の「アダプターパターン」を厳守して実装してください．

1. **プラットフォームの直接判定・呼び出しの禁止:** 
    ReactのUIコンポーネント('src/components'など)の中で`@tauri-apps/api`やブラウザ固有のAPI('localStorage', 'window.showOpenFIlePicker'など)を **絶対に直接インポート・呼び出ししないでください**．

2. **サービスインターフェース(Adapter)の構築:**
    OSやファイルシステムにアクセスする機能(例: ファイル保存，読み込みなど)を実装する際は，必ず`src/services/`または`src/adapters/`ディレクトリに抽象化されたインターフェースを作成してください．
    - 例: `saveFile.ts`という関数をエクスポートし，その内部で実行環境がTauriかWebかを判定(`window.__TAURI__`の有無など)して，処理を分岐させてください．

3. **フォールバック実装:**
    OSやファイルシステムにアクセスする機能(例: ネイティブな高度なテキスト解析など)がある場合は．Web環境で実行されたときの軽量なJavaScriptフォールバック(代替処理)を必ず実装するか，ユーザーに「この機能はデスクトップ版専用です」という適切なUIフィードバックを返すように設計してください．
    
# コーディング規約
- **TypeScript:** `any` 型の使用は厳禁，環境依存を吸収するインターフェースの型定義を最も重要視してください．
- **UI実装:** カスタムHooksを用いてビジネスロジックとUIを分離し，UIコンポーネントは純粋な描画のみを担当するようにしてください．

# レスポンシブ規約
スマートフォン(タッチ)とPC(マウス)の両方で快適に扱えるUIを構築するための以下のルールを順守してください．
- **モバイルファースト・レスポンシブデザイン:** 常にスマホサイズ(狭い画面)での表示を基本として，メディアクエリを用いてPCサイズ(広い画面)に対応する設計(Mobile First)を心がけてください．
- **タッチデバイスへの配慮:** スマホでの利用を想定し，タップ可能な要素(ボタンやリンク)は十分な大きさを持たせてください．また，重要な機能のアクションを「マウスホバー(hover)」のみに依存させないでください．

# 実行・検証コマンド(Self-Verification)
コードを変更した後は，環境を壊していないか以下の両方を考慮して検証してください．
- デスクトップ環境の起動: `npm run tauri dev`
- Web環境の起動: `npm run dev` (またはVide/Next.jsなどのWeb用起動コマンド)
※コード生成時は，常に「これがWebブラウザで開かれたらどう動くか？」を自問自答し，エラーにならない安全な実装を行ってください．

# 注意点
わからないことや最新仕様が必要な場合は、推測で書かずに必ずFetchツールで公式を検索すること